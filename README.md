# YouTube Original Audio

Switches YouTube to the original audio track when you already speak that language.

![Popup](docs/popup.png)

```bash
pnpm install
pnpm build
```

Load `dist/` as an unpacked extension (`chrome://extensions` or Firefox `about:debugging`).
